{
  "name": "transfer-odds",
  "version": "1.0.0",
  "private": true,
  "description": "Transfer admission odds for 588 US colleges, scored against the applicant pool you would actually compete with.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "node test-model.mjs",
    "build": "node build-site.mjs",
    "serve": "node build-site.mjs && npx --yes serve dist",
    "images": "node make-images.mjs",
    "fonts": "node fetch-fonts.mjs"
  },
  "devDependencies": {
    "playwright": "^1.49.1"
  }
}
